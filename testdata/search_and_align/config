# -----------------------------------------------------------------------------
# --commands search,filter,extract,flatten,align
# -----------------------------------------------------------------------------

[General]

# Contact email, so NCBI can inform the user if there are any problems
email = test@test.com

max_cpu_cores_available = 2

usearch6_executable = usearch
mafft_executable = mafft
muscle_executable = muscle

[Hacks]

tgrc = Yes
tgrc_data =

[Flatten]

among_species_similarity = 0.75
within_species_similarity = 0.75

align_program = mafft
align_program_options = --auto

[Align]

align_program = mafft
align_program_options = --auto

order_of_loci = GBSSI, matK-trnK, rbcL, ITS1-5.8-ITS2

number_of_gaps_between_loci = 0
